import { Router, Route } from 'react-router-dom';
import PaginaRespuesta from '../Components/PaginaRespuesta';

const router = () => (
    <Router>
        <Route exact path='/' component={}/> 
        <Route exact path='/result' component={PaginaRespuesta}/> 
    </Router>
)

export default router;